<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--  <mapper namespace="ProductMapper"> 가 @Mapper 와 함께 밑의 한줄로 대체 됨-->
<mapper namespace="com.mvc.forrest.service.RentalReviewMapper"> 	
 	
	<resultMap id="rentalReviewSelectMap" type="rentalReview">
		<result property="reviewNo" 				column="reviewNo" 				jdbcType="INTEGER"/>
		<result property="reviewImg"	   			column="reviewImg" 				jdbcType="VARCHAR" />
		<result property="reviewDetail" 			column="reviewDetail" 			jdbcType="VARCHAR" />
		<result property="reviewScore" 				column="reviewScore" 				jdbcType="VARCHAR"/>
		<result property="prodNo"	   			column="prodNo" 				jdbcType="INTEGER" />
		<result property="userId" 			column="userId" 			jdbcType="VARCHAR" />
		<result property="regDate" 				column="regDate" 				jdbcType="DATE"/>
	</resultMap>
	
	<!-- SQL : INSERT -->
	<insert 	id="addRentalReview"		parameterType="RentalReview" >
		INSERT 
	 	INTO RentalReview(reviewNo, reviewImg, reviewDetail,reviewScore,prodNo,userId,regDate) 
	 	values (#{reviewNo},#{reviewImg},#{reviewDetail},#{reviewScore},#{prodNo},#{userId},sysDate)
	 </insert>
	 
	 
	 <!-- SQL : SELECT ONE -->
	 <select 	id="getRentalReview"	parameterType="int"	resultMap="RentalReviewSelectMap">
		
	 </select>
	 
	 <!-- SQL : UPDATE -->
	 <update	id="updateRentalReview"	parameterType="RentalReview" >
	   	update PRODUCT
	   	<set>
	   		
	   	</set>
	   	where PROD_NO = #{prodNo}
	 </update>
		 
	<!-- SQL : SELECT LIST -->
	<select  id="getRentalReviewList"  parameterType="search"	resultMap="RentalReviewSelectMap">
	  	SELECT *
	  	FROM (	SELECT inner_table.* , ROWNUM AS row_seq
	  					FROM		(	SELECT PRODUCT.*  ,  (PRODUCT.QUANTITY-NVL(A.SUM,0)) SUM
										FROM PRODUCT LEFT OUTER JOIN (SELECT PROD_NO, SUM(purchaseQt) SUM FROM TRANSACTION GROUP BY prod_no) A ON PRODUCT.PROD_NO = A.PROD_NO
											<if test="searchCondition != null">
												<where>
													<if test="searchCondition == 1 and searchKeyword !='' ">
										 				PROD_NO LIKE '%'||#{searchKeyword}||'%' 
													</if>
													<if test="searchCondition == 2 and searchKeyword !='' ">
										 				PROD_NAME LIKE '%'||#{searchKeyword}||'%' 
													</if>
													<if test="searchCondition == 3 and searchKeyword !='' ">
										 				PRICE = #{searchKeyword} 
													</if>
												</where>
											</if>
											<if test="orderCondition != null">
												<if test='orderCondition == "priceAsc"'>
													order by PRICE ASC
												</if>
												<if test='orderCondition == "priceDesc"'>
													order by PRICE DESC
												</if>
												<if test='orderCondition == "prodNoAsc"'>
													order by PRODUCT.PROD_NO
												</if>
											</if> 
											) inner_table
						WHERE ROWNUM &lt;= #{endRowNum} )
		WHERE row_seq BETWEEN #{startRowNum} AND #{endRowNum} 
	 </select>
	 
	
	<!-- SQL : SELECT ROW Count -->	 
	 <select  id="getTotalCount"  parameterType="search"	 resultType="int">
	  	SELECT COUNT(*)
	  	FROM(	SELECT PRODUCT.*  ,  (PRODUCT.QUANTITY-NVL(A.SUM,0)) SUM
				FROM PRODUCT LEFT OUTER JOIN (SELECT PROD_NO, SUM(purchaseQt) SUM FROM TRANSACTION GROUP BY prod_no) A ON PRODUCT.PROD_NO = A.PROD_NO
											<if test="searchCondition != null">
												<where>
													<if test="searchCondition == 1 and searchKeyword !='' ">
										 				PROD_NO LIKE '%'||#{searchKeyword}||'%' 
													</if>
													<if test="searchCondition == 2 and searchKeyword !='' ">
										 				PROD_NAME LIKE '%'||#{searchKeyword}||'%' 
													</if>
													<if test="searchCondition == 3 and searchKeyword !='' ">
										 				PRICE = #{searchKeyword} 
													</if>
												</where>
											</if>
											<if test="orderCondition != null">
												<if test='orderCondition == "priceAsc"'>
													order by PRICE ASC
												</if>
												<if test='orderCondition == "priceDesc"'>
													order by PRICE DESC
												</if>
												<if test='orderCondition == "prodNoAsc"'>
													order by PRODUCT.PROD_NO
												</if>
											</if>) countTable						
	 </select>
	 
	
	 
</mapper>